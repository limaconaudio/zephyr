sample:
  name: DMA Driver Sample
tests:
  test:
    tags: drivers
    harness: console
    harness_config:
        type: multi_line
        ordered: true
        regex:
            - "Test DMA driver"
    depends_on: dma
