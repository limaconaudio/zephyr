menuconfig GPIO_SPINAL_LIB_IO
	bool "SPINAL_LIB_IO GPIO driver"
	depends on GPIO
	select HAS_DTS_GPIO
	help
	  Enable driver for the SPINAL_LIB_IO GPIO controller.

	  Says n if not sure.

if GPIO_SPINAL_LIB_IO


	
endif 
